# Dockerfile for Benchmark Runner
FROM python:3.8-slim

WORKDIR /app
COPY . .
COPY bots ./bots
ENV PYTHONPATH=/app
RUN pip install --upgrade pip && pip install -r requirements-benchmark.txt

CMD ["python", "run_benchmark.py"]
